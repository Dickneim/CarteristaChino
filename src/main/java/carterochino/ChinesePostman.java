package carterochino;

import graphTDA.*;
import java.util.*;

public class ChinesePostman{
    public void ChinesePostmanTour(Grafo grafo){
        Integer<int> ODD = todos los ood grados vertices del grafo;
        if(!ODD.isEmpty()){
            Aristas* = Aristas;
            grafo* = (grafo.getVertices(), Aristas*);
            encuentra el camino más corto entre toods los pares de odd-grados vertices;
            contrustruye a complete bipartite graph H = (U union W, E.), ODD == (v1,...v2k), such that
                U = (u1,...u2k) y ui es una copia de vi;
                W = (wi,...,w2k) y wi es una copia de vi; 
        }
    }
}
